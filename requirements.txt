# Compatível com Python 3.10 a 3.13
# Se a instalação falhar, rode: pip install -r requirements.txt --upgrade

opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# fast-plate-ocr: OCR especializado em placas (CCT - Compact Convolutional Transformer)
# Treinado em 65+ países, usa ONNX Runtime, sem confusão L/D
fast-plate-ocr[onnx]>=1.1.0

ultralytics>=8.0.0
psutil>=5.9.0
